[project]
name="utils"
version="0.1.0"
description = "utils for cookmate"
requires-python = ">=3.13"
dependencies = ["mlflow>=3.11.1"]


[build-system]
requires = ["uv_build>=0.11.13,<0.12"]
build-backend = "uv_build"