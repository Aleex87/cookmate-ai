[project]
name = "cookmate-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
    "pandas>=2.3.3",

]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.workspace]
members = ["cookmate/frontend", "cookmate/backend", "cookmate/utils"]

[tool.uv]
dev-dependencies = [
"ipykernel>=7.2.0",
"pandas>=2.3.3"]
