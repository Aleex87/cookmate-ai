[project]
name="frontend"
version="0.1.0"
description = "frontend for cookmate"
requires-python = ">=3.13"
dependencies = ["streamlit", "httpx", "utils"]


[tool.uv.sources]
utils = { workspace = true }